import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Foundations/Typography" />

# Typography

관리자 UI에서 사용하는 텍스트 스타일입니다.

## Heading

<div style={{ display: "grid", gap: 0 }}>
    <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
        <div
            style={{
            fontSize: 32,
            lineHeight: "40px",
            fontWeight: 700,
            margin: 0,
            }}
        />
        <code>heading-xl</code>
        <span>
            font-size: 32px | line-height: 40px | font-weight: 700 | 타이틀
        </span>
    </div>

    <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
        <div
            style={{
            fontSize: 24,
            lineHeight: "32px",
            fontWeight: 600,
            margin: 0,
            }}
        />
        <code>heading-lg</code>
        <span>
            font-size: 24px | line-height: 32px | font-weight: 600 | 타이틀
        </span>
    </div>

    <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
        <div
            style={{
            fontSize: 20, lineHeight: "28px", fontWeight: 600, margin: 0
            }}
        />
        <code>heading-md</code>
        <span>
            font-size: 20px | line-height: 28px | font-weight: 600 | 타이틀
        </span>
    </div>

</div>

## Body

<div style={{ display: "grid", gap: 0 }}>
    <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
        <div
            style={{
            fontSize: 16,
            lineHeight: "24px",
            fontWeight: 400,
            margin: 0,
            }}
        />
        <code>body-lg</code>
        <span>
            font-size: 16px | line-height: 24px | font-weight: 400 | 본문
        </span>
    </div>

    <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
        <div
            style={{
            fontSize: 15,
            lineHeight: "22px",
            fontWeight: 400,
            margin: 0,
            }}
        />
        <code>body-md</code>
        <span>
            font-size: 15px | line-height: 22px | font-weight: 400 | 본문
        </span>
    </div>

    <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
        <div
            style={{
            fontSize: 14, lineHeight: "20px", fontWeight: 400, margin: 0
            }}
        />
        <code>body-sm</code>
        <span>
            font-size: 14px | line-height: 20px | font-weight: 400 | 본문
        </span>
    </div>

</div>

## Label

<div style={{ display: "grid", gap: 0 }}>
  <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
    <div
      style={{
        fontSize: 13,
        lineHeight: "18px",
        fontWeight: 500,
        margin: 0,
      }}
    />
    <code>label-md</code>
    <span>
      font-size: 13px | line-height: 18px | font-weight: 500 | 버튼, 폼, 라벨
    </span>
  </div>
</div>

## Caption

<div style={{ display: "grid", gap: 0 }}>
  <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
    <div
      style={{
        fontSize: 12,
        lineHeight: "16px",
        fontWeight: 400,
        margin: 0,
      }}
    />
    <code>caption-xs</code>
    <span>
      font-size: 12px | line-height: 16px | font-weight: 400 | 보조 정보
    </span>
  </div>
</div>
